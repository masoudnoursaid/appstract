# Minio Terraform

Place your kubeconifg file in current path then :

```sh
terraform init
terraform plan
terraform apply
```

Terraform apply with yes :

```sh
terraform apply -auto-approve
```

Destroy :

```sh
terraform destroy
```
